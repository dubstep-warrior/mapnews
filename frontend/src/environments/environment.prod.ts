const base_api = 'backend.dubstep-warrior.com/app/mapnews';

export const environment = {
  production: true,
  endpoint_mapnews_backend_api: `https://${base_api}`,
  ws_endpoint_mapnews_backend_api: `wss://${base_api}/ws`,
};
