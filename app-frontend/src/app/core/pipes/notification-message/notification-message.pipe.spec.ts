import { NotificationMessagePipe } from './notification-message.pipe';

describe('NotificationMessagePipe', () => {
  it('create an instance', () => {
    const pipe = new NotificationMessagePipe();
    expect(pipe).toBeTruthy();
  });
});
