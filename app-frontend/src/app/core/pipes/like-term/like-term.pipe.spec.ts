import { LikeTermPipe } from './like-term.pipe';

describe('LikeTermPipe', () => {
  it('create an instance', () => {
    const pipe = new LikeTermPipe();
    expect(pipe).toBeTruthy();
  });
});
